<%before%>
\begin{pmatrix}
    {% for row in matrix  %}
        {% for element in row %}
            <%element%> {% if (loop.last) %} {% else %} & {% endif %}
        {% endfor %}
        \\
    {% endfor %}
\end{pmatrix}
<%after%>